pipeline {
    agent any
    stages {
        stage ('checkout') {
            echo "checking the source code"
        }

        stage ('Build') {
            steps {
                echo "building..."
            }
        }

        stage('test') {
            steps {
                echo 'testing...'
            }
        }
    }
}